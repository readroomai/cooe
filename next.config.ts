import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The floating dev badge sits on top of the hero and ends up in screenshots.
  devIndicators: false,
};

export default nextConfig;
